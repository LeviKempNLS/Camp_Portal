# PostgreSQL connection for the Prisma schema. Never commit a real connection string.
DATABASE_URL="postgresql://camp_portal:local_only@localhost:5432/camp_portal?schema=public"
# Select adapters without leaking secrets into application configuration.
PAYMENT_PROVIDER="stripe"
EMAIL_PROVIDER="resend"
STORAGE_PROVIDER="s3"
