import { listRegistrarRegistrations, AuthorizationError } from "@faith-adventures/database/portal";
import { requirePortalUser } from "../lib/access";
import { notFound } from "next/navigation";
export default async function AdminPage(){ const user=await requirePortalUser(); let rows; try { rows=await listRegistrarRegistrations(user.id); } catch (error) { if(error instanceof AuthorizationError) notFound(); throw error; } return <main className="shell"><p className="eyebrow">Registrar workspace</p><h1>Demo registration queue</h1><section className="table-card"><div><h2>Registrations</h2><p>Fictitious development records only. Full form answers are never shown in this queue.</p></div><table><thead><tr><th>Camper</th><th>Session</th><th>Status</th><th>Updated</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.person.firstName} {r.person.lastName}</td><td>{r.session.name}</td><td>{r.status}</td><td>{r.updatedAt.toLocaleDateString()}</td></tr>)}</tbody></table></section></main> }
