import Link from "next/link";
import { RegistrationWizard } from "../../../components/registration-wizard";
import { getPrismaClient } from "@faith-adventures/database";
import { getOwnedHousehold, loadOwnedDraft } from "@faith-adventures/database/portal";
import { requirePortalUser } from "../../lib/access";
export default async function NewRegistrationPage({searchParams}:{searchParams:Promise<{camperId?:string}>}) { const user=await requirePortalUser(); const household=await getOwnedHousehold(user.id); const campers=household.members.filter(m=>m.relationship==="CAMPER"); const camperId=(await searchParams).camperId; if(!camperId)return <main className="shell"><h1>Select a camper</h1><ul>{campers.map(c=><li key={c.personId}><Link href={`/registrations/new?camperId=${c.personId}`}>{c.person.firstName} {c.person.lastName}</Link></li>)}</ul></main>; const camper=campers.find(c=>c.personId===camperId); if(!camper)return <main><h1>Camper unavailable</h1></main>; const session=await getPrismaClient().session.findFirst({where:{status:"open"},orderBy:{startDate:"asc"}}); if(!session)return <main><h1>Registration unavailable</h1></main>; const draft=await loadOwnedDraft(user.id,session.id,camper.personId); return <main className="shell narrow"><h1>Register {camper.person.firstName}</h1><RegistrationWizard sessionId={session.id} camperId={camper.personId} initialAnswers={(draft?.answers??{}) as Record<string,string|boolean>}/></main>; }
