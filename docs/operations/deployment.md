# Deployment

Deploy the portal separately from Wix at `portal.faithadventurescamp.org`. Supply PostgreSQL, object storage, authentication, payment, email, and application secrets through the deployment platform's secret manager. Apply reviewed, source-controlled migrations before application rollout. Run the CI checks on every pull request.
